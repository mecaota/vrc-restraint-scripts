# MovePositionByContact

## 概要

このコンポーネントをセットすると、Contactがトリガーされた際にそのContactした位置から一番近いプレイヤーのボーンとそのプレイヤーをPlayerBoneConstraintに設定します。

PlayerBoneConstraintとセットで利用します。

## 設定項目

### Constraints

Contactした位置にConstraintさせるPlayerBoneConstraintスクリプトを指定します。PlayerBoneConstraintが追加されたオブジェクトを指定します。
